import { useState } from "react";

export default function Example() {
  return (
    <div className="example">
      <button type="button" className="clear-cv">
        🗑️ Clear CV
      </button>
      <button type="button">Load Example</button>
    </div>
  );
}
